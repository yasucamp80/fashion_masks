<!DOCTYPE html>
<html>
  <head>
    <title>FOMS(Fashion of Masks)</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header class="header">
      <div class="header__bar row">
        <h1 class="grid-6"><a href="/">FOMS</a></h1>
        <% if user_signed_in? %>
          <div class="user_nav grid-6">
            <span><%= current_user.nickname %>
              <ul class="user__info">
                <li>
                  <%= link_to "my page", "/users/#{current_user.id}" %>
                  <%= link_to "log out", destroy_user_session_path, method: :delete %>
                </li>
              </ul>
            </span>
            <%= link_to "post", new_tweet_path, class: "post" %>
          </div>
        <% else %>
          <div class="grid-6">
            <%= link_to "log in", new_user_session_path, class: "post" %>
            <%= link_to "new", new_user_registration_path, class: "post" %>
          </div>
        <% end %>
      </div>
    </header>
      <%= yield %>
    <footer>
      <small>Copyright FOMS(Fashion of Masks) 2020.<br>
      <small>The FOMS is not responsible for the content of external sites.</small>
    </footer>
  </body>
</html>